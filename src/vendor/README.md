# vendor/fast-jev-compaction

https://github.com/tamaratran/fast-jev-compaction (MIT License, v0.2.0) の `src/` をそのまま同梱している。
opencode の `context` フックから直接 import するため vendored。更新時は upstream の `src/` をコピーし直す。
